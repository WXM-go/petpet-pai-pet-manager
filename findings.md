# 当前版本问题盘点调查笔记

## 调查范围

- 只读检查当前工作区源码、资源、测试、构建配置、发布产物和团队协作约定。
- 本次不修改代码、资源或配置。

## 已确认基线

- 当前仓库尚未产生首个 Git 提交，工作区存在完整源码、测试、资源和 `release` 目录。
- `task_plan.md` 声称自动测试 46/46、Windows x64 便携版启动冒烟通过；需要结合当前源码和可执行检查复核，不能只把计划记录当作现状证明。

## 调查错误

| 错误 | 尝试 | 处理 |
|---|---|---|
| PowerShell 变量插值在字符串 `"$p:$start-$end"` 中报错 | 第一次分段读取 `src/main.js` | 改用 `${p}` 明确变量边界后重试 |
| 当前 CUA 入口不提供原生应用 `getApp` 方法 | 尝试启动现有 Windows 便携版做窗口截图验证 | 停止桌面自动化，改用源码、资源、发布目录和测试证据；未做实机视觉截图 |
| PowerShell 不支持所用 heredoc 重定向语法 | 第一次执行 manifest/源码合同检查 | 改用单行 Node 只读检查 |
| 单行 Node 检查中的嵌套引号转义错误 | 合同检查第一次重试 | 保留已获得的 `rg` 行号证据，后续改用 PowerShell 原生解析 |

## 发现

- 测试基线为 46/46 通过，但测试主要覆盖纯函数、资源报告和静态 UI 契约；没有发现主进程 IPC、真实导入/删除事务、窗口生命周期或 Windows 集成测试。
- `src/main.js` 负责大量同步文件操作与窗口/系统集成，文件约 608 行；导入流程在复制失败时没有事务回滚，ZIP 在完整解压后才验证 manifest 和资源。
- `src/pet-library.js` 只校验 manifest 字段和相对路径，不校验精灵图实际尺寸、8×11 图集结构、预览图或 GIF 的格式/大小。
- 管理器前端把 9 个标准动作和 4 个互动按钮写死，没有按桌宠包的 `actions`、`care.interactions` 或能力声明裁剪。
- `care-model.js` 只真正执行 `light-care`；`full-care` 只是被接受并展示为“后续扩展”，属于已声明但未实现的能力。
- 软团团 manifest 使用 `care.animations.sleep`，而运行时在睡眠状态查找 `animationUrls.sleeping`，睡眠专属动画不会被播放。
- 软团团已有资源 QA 记录透明残留为 0、图集尺寸为 1536×2288，但当前运行时仍依赖固定 192×208 单元和固定 8×11/16 方向，导入校验没有把这些条件变成硬门槛。
- `main.js` 未见单实例锁、显示器/DPI 变化处理、渲染器崩溃恢复或发布产物清理约束；键盘监听通过 PowerShell 全局低级键盘钩子启动，且使用 `ExecutionPolicy Bypass`。
- `manager-renderer.js` 在每次状态更新时销毁并重建全部卡片和缩略图；轻养成每秒推送一次状态，桌宠数量增长后会产生重复图片加载和 DOM 重建。
- 管理器没有把暂停状态放进 `manager-state`，因此“暂停 / 继续”按钮没有当前状态反馈；导航按钮有 active 样式但点击滚动后不会更新 active 状态。
- 导入错误会直接把 `normalizeManifest` 的英文异常消息显示给用户，预览初始化失败则静默吞掉异常，中文错误体验不完整。
- 资源 QA 的 `look-continuity.json` 标记 `reviewRequired: true`，并指出 `157.5→180` 与 `337.5→000` 为局部差异异常；`sleep.gif` 与 `feed.gif` SHA-256 完全相同，且 manifest 使用 `sleep` 键而运行时睡眠状态查找 `sleeping`。
- 当前 `release/派派桌宠管理器-win32-x64` 目录包含 `pets/` 和 `petpet-state.json`，其中 state 已记录 50% 缩放和变化后的养成值；虽然打包忽略规则写了这些路径，但没有清理旧发布目录的证据，存在把本地运行状态带入交付目录的风险。
- 依赖离线高危审计当前为 0，但没有形成发布脚本中的自动审计/产物验证门槛；`package:win` 只有打包动作，没有清理、启动冒烟、完整包内容或哈希清单步骤。
- 工作区未发现 `CHANGELOG.md`、许可证、发布记录、测试计划、回归报告、兼容性矩阵或 `pet.schema.json`；`README.txt` 与使用说明能指导基本使用，但不能替代发布证据和协议文档。
- `package:win` 生成的是 `release` 下的目录，没有压缩归档、产物哈希或自动验证步骤；当前 release 目录还残留运行态 `pets/` 与 `petpet-state.json`。
- 本地 contact sheet 视觉检查显示标准动作和方向图集已正确分格、透明背景正常；但这不能替代真实 Electron 缩放/窗口截图，方向闭环的两个 QA 警告仍应视为待复核项。
