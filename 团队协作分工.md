# 派派桌宠管理器团队协作分工

## 一、协作方式

当前主对话是项目总协调台，负责确定优先级、分配任务、合并结果和最终验收。其余岗位各自使用独立对话，只处理自己的职责范围，涉及其他岗位时通过“输入、输出、依赖、风险”交接，不直接替别人改变设计或接口。

当前项目的 Git 仓库尚未产生首个提交，因此岗位对话暂时使用项目本地模式，不创建独立 worktree。首个稳定版本提交后，如果某个岗位需要并行修改代码，再为该岗位单独建立 worktree，并由总协调台负责合并。

每个岗位对话的默认工作顺序：

1. 先读取本项目源码、执行记录和本分工表。
2. 先说明现状、问题、方案和影响范围，再开始修改。
3. 先写或补充可验证的测试/验收条件，再实现功能或制作资源。
4. 完成后报告改动、验证结果、未解决问题和需要其他岗位配合的事项。
5. 不执行桌宠包中的任意脚本，不擅自扩大产品范围，不覆盖其他岗位未交付的工作。

每个岗位对话完成一项任务后，必须在回复末尾给出下一步指引，不能只报告“已完成”。统一使用以下结构：

```text
【本次完成】
已经完成的工作和交付物。

【未完成/风险】
仍未解决的问题；如果没有，填写“无”。

【下一步】
必须明确写出：下一步应进入的岗位对话的完整名称、具体要做的任务、进入条件、交付物和验收目标。不能只写“继续优化”“进入开发”“做下一版本”等模糊方向。

【可直接发送的指令】
给下一个岗位对话使用的完整中文指令，必须包含任务范围、需要读取的资料、禁止越界事项和完成标准。
```

如果当前任务尚未达到完成定义，应先说明继续完成当前任务所需的动作，不得跳到后续阶段。只有当前岗位交付物和验收条件明确后，才能推荐下一个岗位。

岗位交接的判断规则：

- 盘点、需求、优先级或路线图完成后，下一站通常是承担最高优先级事项的岗位，而不是笼统地说“开始开发”。
- 若最高优先级事项有多个前置依赖，先进入最上游依赖岗位；本次必须说明为什么先去该岗位。
- 若当前任务只完成了一部分，下一步必须继续留在当前岗位对话，并写明剩余交付物；不得假装已经可以流转。
- 每次任务结束时，岗位对话必须在最后一段重复一次“下一站：岗位完整名称 → 任务名称”，确保用户不用从长篇报告中自行推断。

## 二、岗位与对话

### 1. 产品与项目管理

负责把想法变成需求、里程碑和可验收任务，维护优先级、版本范围、风险和跨岗位依赖。

专业技能：产品设计、需求分析、项目管理、用户研究、验收标准、风险管理、桌面软件基础。

主要交付：产品需求文档、版本路线图、任务拆分、优先级、验收清单、决策记录。

不负责：直接实现代码、绘制最终桌宠资源、替 QA 宣布发布。

### 2. UX/UI 与视觉设计

负责中国用户习惯下的界面结构、交互流程和视觉设计系统，保证界面不暗示只支持像素风。

专业技能：用户体验、信息架构、交互设计、视觉设计、设计系统、无障碍、中文界面文案。

主要交付：页面结构、组件状态、颜色字体规范、交互说明、空/错/加载状态、UI 验收标准。

不负责：桌宠精灵图、Electron 窗口底层、业务状态持久化。

### 3. Electron 桌面平台

负责主进程、预加载脚本、窗口生命周期、透明置顶、拖动、托盘、右键菜单、文件选择和 IPC。

专业技能：Electron、Node.js、Windows 窗口机制、IPC、安全边界、文件系统、进程和发布诊断。

主要交付：稳定的桌面窗口能力、IPC 协议、系统集成、平台错误处理、Windows 兼容性报告。

不负责：管理器具体页面布局、桌宠动作资源、产品需求取舍。

### 4. 管理器前端

负责管理器窗口中的桌宠库、搜索筛选、卡片、详情控制台、导入流程、动作预览和中文交互。

专业技能：HTML、CSS、JavaScript、状态管理、组件化、表单交互、键盘可用性、视觉还原。

主要交付：可用的管理器页面、状态反馈、前端接口适配、UI 自动化/合约测试、页面性能报告。

不负责：直接访问系统文件、修改桌宠包协议、生成桌宠美术资源。

### 5. 桌宠运行时引擎

负责桌宠在桌面上的渲染、动作、方向、尺寸、交互、自动演示、暂停和轻养成运行逻辑。

专业技能：Canvas、动画播放、精灵图、坐标与锚定、输入事件、状态机、性能优化、JavaScript 测试。

主要交付：渲染器、动作播放规则、缩放规则、互动分层、睡眠保护、运行时状态接口和单元测试。

不负责：桌宠包资源制作、管理器视觉设计、Windows 安装流程。

### 6. 桌宠标准与工具链

负责 `pet.json` v2、桌宠包目录、能力声明、导入校验、旧包兼容和资源生产工具链。

专业技能：JSON Schema、资源规范、文件校验、CLI/脚本、版本兼容、构建流水线、安全设计。

主要交付：manifest 规范、校验器、图集组装/检查工具、桌宠包打包流程、兼容性矩阵和工具测试。

不负责：执行桌宠包脚本；首版只允许声明能力，不允许第三方包获得任意系统权限。

### 7. 桌宠美术与动画

负责软团团及其他画风桌宠的角色设计、动作设计、精灵图、互动动画、预览图和图标素材。

专业技能：角色设计、插画/像素/3D/手绘等风格制作、逐帧动画、透明抠图、精灵图排版、视觉 QA。

主要交付：角色设定、动作分镜、标准 8×11 图集、16 方向、互动动画条、透明边缘检查和资源说明。

不负责：管理器 UI；不能为了适配某一种画风而破坏通用桌宠包标准。

### 8. 测试与质量保障

负责从资源、manifest、运行时、管理器到 Windows 便携版的自动化、视觉和回归测试。

专业技能：测试设计、Node 测试、可控时钟、UI/集成测试、资源 QA、Windows 冒烟、问题定位和发布阻断判断。

主要交付：测试计划、用例、回归报告、视觉检查记录、Bug 报告、发布阻断清单。

不负责：为了让测试通过而降低产品要求；发现问题时必须保留复现步骤和证据。

### 9. 发布、文档与本地化

负责版本号、Windows 10/11 x64 便携版、应用图标、构建产物、用户说明、桌宠包制作说明和中文文案一致性。

专业技能：Windows 发布、Electron 打包、版本管理、技术写作、简体中文本地化、用户支持和回滚流程。

主要交付：发布包、产物清单、更新日志、用户文档、中文文案审核表、发布/回滚记录。

不负责：在 QA 未通过时宣布版本完成；不擅自修改功能逻辑来掩盖发布问题。

## 三、推荐开发顺序

```text
产品与项目管理
        ├── UX/UI 与视觉设计
        ├── Electron 桌面平台
        └── 桌宠标准与工具链
                ├── 桌宠运行时引擎
                └── 桌宠美术与动画

UX/UI + Electron + 运行时 + 工具链 + 美术
        └── 管理器前端与桌面集成
                └── 测试与质量保障
                        └── 发布、文档与本地化
```

推荐阶段：

1. 产品基线：明确目标用户、首版范围、桌宠包边界、优先级和验收标准。
2. 体验与架构：确定页面信息架构、视觉规则、窗口模型、IPC 和数据流。
3. 标准先行：稳定 `pet.json`、图集尺寸、动作映射、渲染模式和兼容策略。
4. 核心能力：实现 Electron 平台和桌宠运行时，先保证窗口、渲染、输入、互动可靠。
5. 内容生产：按标准制作软团团，再制作像素、插画、3D、手绘等示例桌宠。
6. 管理器整合：完成桌宠库、导入、详情控制、预览、设置和错误反馈。
7. 质量验收：完成自动测试、视觉 QA、性能、边界、旧包和 Windows 冒烟测试。
8. 发布交付：生成便携版、检查图标和资源、完善文档、写更新日志并记录回滚点。

## 四、岗位交接格式

每次岗位交接都使用以下格式，避免只说“完成了”：

```text
【岗位】
【任务】
【现状】
【本次交付】
【修改文件/资源】
【对外接口】
【验证命令与结果】
【已知问题】
【依赖的其他岗位】
【下一步】
```

## 五、统一完成定义

一个岗位任务只有同时满足以下条件才算完成：

- 交付物位于项目工作区，并且有明确的文件/资源清单。
- 对外接口、输入输出、兼容行为和失败提示已经写清楚。
- 有对应的自动测试、资源检查或可重复的手工验收步骤。
- 不影响已有的中文界面、旧桌宠兼容、多画风渲染和 Windows 便携版能力。
- 已知限制和后续工作已交给项目总协调台，不把未完成内容描述成已完成。

## 六、统一对话交接要求

每项岗位工作完成或被阻塞时，交付方必须额外明确说明：

1. 下一步应转到哪个岗位对话或哪个已有对话执行操作。
2. 下一步具体要执行的操作、前置条件和验证目标。
3. 一段可以直接复制到下一对话的完整提示词。

如果当前任务尚未真正完成，也必须说明当前停在哪个对话、由哪个岗位继续，以及未完成的验证或风险；不能只报告“已完成”而省略后续去向。该要求适用于本项目所有岗位、阶段和回归任务。
