# 派派桌宠管理器发布记录

## 版本信息

- 版本：`1.0.0`
- 平台：Windows 10/11 x64
- 形态：便携版
- Electron：44.3.0
- 构建时间：2026-09-16 16:38:58
- 发布状态：已通过 QA，完成最终本地发布交付

## 最终产物

```text
D:\petpet派派桌宠管理器\release-final-delivery\派派桌宠管理器-win32-x64\
└─ 派派桌宠管理器.exe
```

目录包含 73 个文件：顶层 Electron 运行文件 17 个、`locales` 语言包 55 个、`resources\app.asar` 1 个。`app.asar` 包含应用入口、三个页面、应用图标、软团团 `pet.json`、精灵图、预览图和互动资源。

## 构建步骤

1. 确认 `package.json` 和 `package-lock.json` 版本均为 `1.0.0`。
2. 保留原 `release` 目录和已通过 QA 的 `release-final` 目录，不删除其中的旧产物和用户数据。
3. 从 QA 通过的 `release-final` 按白名单复制到 `release-final-delivery`。
4. 排除测试目录、QA 用户数据、缓存、运行态文件、开发计划和异常环境目录。
5. 检查 EXE、`resources\app.asar`、应用图标和软团团资源。
6. 清点发布目录，并对 `app.asar` 做开发文件和运行态文件检查。
7. 使用 QA 已记录的全新可写 `--user-data-dir` 回归证据作为交付依据。

## 回滚步骤

1. 停止分发 `release-final-delivery`。
2. 保留候选包、哈希和 QA 日志，便于定位问题。
3. 继续使用上一份已验收的便携版；不要覆盖或删除原 `release` 或 `release-final` 目录。
4. 若问题来自本次发布包，停止分发 `release-final-delivery`，修复后生成新的交付目录，再执行完整验收。
5. 若问题来自用户数据，只处理对应用户数据目录，不删除项目源代码、旧产物或其他用户数据。

## 验证结果

- 版本号：已核对为 `1.0.0`。
- EXE：存在，路径正确。
- `resources\app.asar`：存在，运行所需入口和资源存在。
- 应用图标：`assets\app-icon.ico` 存在，并已包含在 `app.asar`。
- 桌宠资源：软团团 `pet.json`、`spritesheet.webp`、`preview.png` 和 5 个互动 GIF 存在。
- 发布目录：73 个文件、2 个目录；`app.asar` 共 270 个条目；未发现测试目录、QA 临时目录、缓存、运行态文件或开发计划文件。
- 原生窗口 UI、按钮 IPC、互动、自动演示、预览和正常退出：QA 记录为通过。

## 哈希

- EXE SHA-256：`880BBD7562C2A2389DABDD4765EE368CD3F905D18DC4E335C5D0FC34131D93E9`
- `app.asar` SHA-256：`B429F20EA74855602BA9013FF18D2BB86C9F8076F104372E810B57C1E1A88D23`
