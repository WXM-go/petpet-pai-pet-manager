# 派派桌宠管理器发布记录

## 版本信息

- 版本：`1.1.0`
- 平台：Windows 10/11 x64
- 形态：便携版
- Electron：44.3.0
- 构建时间：2026-09-17 22:03:03
- 发布状态：已通过人工 QA，正式包已完成本地验收，并已创建 GitHub Release

## 最终产物

```text
D:\petpet派派桌宠管理器\release-v1.1.0\派派桌宠管理器-win32-x64\
└─ 派派桌宠管理器.exe

D:\petpet派派桌宠管理器\release-v1.1.0\派派桌宠管理器-v1.1.0-Windows-x64.zip
```

GitHub Release：<https://github.com/WXM-go/petpet-pai-pet-manager/releases/tag/v1.1.0>。上传资产名为 `petpet-manager-v1.1.0-windows-x64.zip`。

目录包含 73 个文件：顶层 Electron 运行文件 17 个、`locales` 语言包 55 个、`resources\app.asar` 1 个。`app.asar` 包含 43 个条目，包括应用入口、三个页面、应用图标、软团团 `pet.json`、精灵图、预览图和互动资源。

## 构建步骤

1. 将 `package.json` 和 `package-lock.json` 版本统一为 `1.1.0`。
2. 保留原 `release`、`release-final` 和 `release-final-delivery` 目录，不删除其中的旧产物和用户数据。
3. 从当前工作区代码重新生成独立的 `release-v1.1.0` 便携版。
4. 排除测试目录、QA 用户数据、缓存、运行态文件、开发计划和异常环境目录。
5. 检查 EXE、`resources\app.asar`、应用图标和软团团资源。
6. 清点发布目录，并对 `app.asar` 做开发文件和运行态文件检查。
7. 使用 QA 已记录的全新可写 `--user-data-dir` 回归证据作为交付依据。

## 回滚步骤

1. 停止分发 `release-v1.1.0`。
2. 保留候选包、哈希和 QA 日志，便于定位问题。
3. 继续使用上一份已验收的便携版；不要覆盖或删除原 `release` 或 `release-final` 目录。
4. 若问题来自本次发布包，停止分发 `release-v1.1.0`，回退到上一份已验收便携版，修复后生成新的交付目录，再执行完整验收。
5. 若问题来自用户数据，只处理对应用户数据目录，不删除项目源代码、旧产物或其他用户数据。

## 验证结果

- 版本号：`package.json`、`package-lock.json`、app.asar 内 `package.json`、EXE 文件版本均为 `1.1.0`。
- EXE：存在，路径正确。
- `resources\app.asar`：存在，运行所需入口和资源存在。
- 应用图标：`assets\app-icon.ico` 存在，并已包含在 `app.asar`。
- 桌宠资源：软团团 `pet.json`、`spritesheet.webp`、`preview.png` 和 5 个互动 GIF 存在。
- 发布目录：73 个文件、2 个目录；`app.asar` 共 43 个条目；未发现测试目录、QA 临时目录、缓存、运行态文件或开发计划文件。
- 原生窗口 UI、按钮 IPC、互动、自动演示、预览和正常退出：QA 记录为通过。

## 哈希

- EXE SHA-256：`A1F30B3DA19F497DE083383AA5615DF8422F7B14CD6D68266130E0B2A88E23E3`
- `app.asar` SHA-256：`3DA1F4A6B324B69734B289078169E4048401339492076F765FD98AA9E81AA3FD`
- ZIP SHA-256：`46C0607DA4ABF6A5C7EC9F797539E4DF79B99F6D4F4DE181572CA2A01E3F4177`
